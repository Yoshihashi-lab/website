# ページレイアウトの改修計画 (Full-width Headers & Compact Content)

## 目標
見出し（ページタイトル等）は全画面の横幅いっぱいに表示し、それ以外の各コンテンツ（カード、文章、マップなど）は中央にコンパクト（最大幅1100px程度）にまとめるレイアウトに変更します。

## 具体的な変更点

1. **全体幅の最適化 (`style.css`)**
   - 現在 `--max-width: 100%;` となっている設定を `--max-width: 1100px;`（または 1000px）に変更し、コンテンツが中央にまとまるようにします。
2. **ページ見出しの全画面化 (`style.css`)**
   - サブページのヘッダー（`.page-header`）のCSSを改修し、親要素（`.container`）の幅制限を無視して画面の端から端まで広がるデザイン（背景色付きの帯）に変更します。
   - 文字は中央揃えにし、リッチな印象のフルスクリーンバナー型にします。
3. **トップページ (`index.html`, `en/index.html`) のセクション背景**
   - 必要に応じて、特定のセクション背景（`.map-section` の薄いグレー背景など）が画面全体に広がるようにCSSを調整します。

これにより、HTMLファイルを一つずつ書き直すことなく、CSSの変更のみで全てのページにこの「見出し全画面＋コンテンツコンパクト」のレイアウトを適用できます。

## User Review Required
- コンテンツの最大幅は 1100px を想定していますが、これでよろしいでしょうか？（より狭い・広い方が良い場合はお知らせください）
- サブページの見出し（News, Study など）の背景色は、研究室のテーマカラー（濃いブルーからグレーへのグラデーション）を全画面に敷く形を想定しています。
